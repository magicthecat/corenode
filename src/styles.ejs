<style>
    /* Reset default margin and padding for all elements */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    /* Body styles */
    body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        background-color: #f5f5f5;
        color: #333;
    }

    /* Top navigation styles */
    nav {
        background-color: #333;
        padding: 10px 0;
    }

    nav ul {
        list-style: none;
        display: flex;
        justify-content: center;
    }

    nav li {
        margin-right: 20px;
    }

    nav a {
        color: #fff;
        text-decoration: none;
        padding: 10px;
        font-size: 16px;
        /* Adjust the font size for navigation items */
    }

    nav a:hover {
        background-color: #555;
    }

    /* Page content styles */
    .container {
        max-width: 960px;
        margin: 0 auto;
        padding: 20px;
    }

    .title-container {
        background-color: #dfd156;
        color: black;
        font-size: larger;
        padding: 10px;
        margin-left: 5%;
        border-bottom: 4px solid #1f1d1d;
    }

    /* Content styles */
    .content-container {
        border: 2px solid #ccc;
        border-radius: 5px;
        padding: 10px;
        margin: 10px 0;
    }

    /* Responsive styles */
    @media (max-width: 768px) {
        nav ul {
            flex-direction: column;
            align-items: center;
        }

        nav li {
            margin-right: 0;
            margin-bottom: 10px;
        }

        nav a {
            font-size: 20px;
            /* Adjust the font size for navigation items on mobile */
        }

        .container {
            padding: 10px;
        }

        .title-container {
            margin-left: 0;
        }
    }

    /* Tablet styles */
    @media (max-width: 1024px) {
        .container {
            max-width: 768px;
        }
    }
</style>