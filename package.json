{
  "name": "corenode",
  "version": "1.0.0",
  "description": "A basic web application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node modules/server.js",
    "build": "node modules/build.js",
    "dev": "nodemon -e ejs --exec \"npm run build && npm run start\"",
    "deploy": "node modules/build.js firebase deploy"
  },
  "author": "magicthecat",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}